logging:
  file:
    name: logs/servicea.log
  level:
    root: INFO
    com.company: DEBUG

server:
  port: 8081

spring:
  application:
    name: service-a

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

# Spring Cloud Gateway’s discovery locator, the gateway automatically adds
# a predicate like Path=/service-name/**